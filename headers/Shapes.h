#ifndef SHAPES_H
#define SHAPES_H

#include "Junction.h"

/*struct Point2D
{
    int x, y;
};

struct Point3D
{
    int x, y, z;
};

struct Line2D
{
    Point2D x, y;
};

void drawTriangle(Point2D p1, Point2D p2, Point2D p3);
void drawCube(Point3D p, int cubeSize);
//void drawQuad(int x, int y, int z, int diagonal);
*/

#endif // SHAPES_H
